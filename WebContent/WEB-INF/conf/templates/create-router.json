{
    "kind": "Route",
    "apiVersion": "v1",
    "metadata": {
        "name": "${id}"
    },
    "spec": {
        "host": "${cm-router-prefix}",
        "to": {
            "kind": "Service",
            "name": "${cm-svc-name}"
        },
        "port": {
            "targetPort": "9000-tcp"
        }
    }
}
